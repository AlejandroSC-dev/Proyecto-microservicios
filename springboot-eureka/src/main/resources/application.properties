server.port=8761

spring.application.name=springboot-eureka

#El servidor no se registra asi mismo
eureka.client.register-with-eureka=false 

#No necesita consultar otros servicios
eureka.client.fetch-registry=false
